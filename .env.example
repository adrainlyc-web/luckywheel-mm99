# Password for the /admin.html dashboard. Pick anything you like.
ADMIN_PASSWORD=change-me

# Set automatically by Vercel once you add a Postgres database in the
# Storage tab (Neon/Postgres integration) — you don't need to set this
# by hand in production. For local testing with `vercel dev`, paste the
# connection string Vercel gives you here.
POSTGRES_URL=
